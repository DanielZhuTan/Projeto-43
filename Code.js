const accessCode1 = "VARIÁVEL";
const accessCode2 = "FUNÇÃO";
const accessCode3 = "BANCO DE DADOS";

function clues() {
  fill("white");
  textSize(15);
  fill("lightblue");
  text("Dica: Sempre mudando, não constante!", 100, 70);

  // Adicione código para exibir as outras duas perguntas.
  fill("white");
  textSize(15);
  fill("lightblue");
  text("Fica cada vez mais molhado quanto mais a gente seca", 600, 170);
  // adicione código para exibir as outras perguntas.
  fill("white");
  textSize(15);
  fill("lightblue");
  text("É feito de água, mas se for colocado dentro da água morrerá", 100, 300);
}
